source 'https://rubygems.org'

source 'file:///home/rog-usr/repo' if File.exists?("/home/rog-usr/repo") # If there's a custom gem repository available, use it.

gem 'rubyongo'

group :development, :test do
  gem "bundler"
  gem "rake"
  gem "minitest"
end

group :development do
end
